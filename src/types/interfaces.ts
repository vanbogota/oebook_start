export interface Locker {
    id: string;
    name: string;
}

export interface PostDeliveryProvider {
    id: string;
    name: string;
}

export interface Library {
    id: string;
    name: string;
}