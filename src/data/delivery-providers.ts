import { PostDeliveryProvider } from "@/types/interfaces";

const DELIVERY_PROVIDERS: PostDeliveryProvider[] = [
  { id: 'posti', name: 'Posti' },
  { id: 'post-nord', name: 'PostNord' },
];

export default DELIVERY_PROVIDERS;